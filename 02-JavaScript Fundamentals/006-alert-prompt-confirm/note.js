'use strict'
// The word “modal” means that the visitor can’t interact with the rest of the page, press other buttons, etc, until they have dealt with the window

alert("message"); // shows a message and waits for the user to press “OK”.

let result = prompt("title", "An optional second parameter, the initial value for the input field."); // shows a modal window with a text message, an input field for the visitor, and the buttons OK/Cancel.

let isConfirmed = confirm("question") // shows a modal window with a question and two buttons: OK and Cancel. The result is true or false.



